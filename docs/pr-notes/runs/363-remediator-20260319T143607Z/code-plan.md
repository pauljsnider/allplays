Code role
Inspect current share button handler and media loading path in team-chat.html.
Add minimal state needed to cache a shareable File/blob before the click.
Keep fallback-to-copy behavior when share/file support is unavailable or cache preparation fails.
