## Plan

1. Adjust the requested-game gate in `js/game-day-entry.js` so explicit completed games remain selectable.
2. Update `tests/unit/game-day-entry.test.js` with one positive completed-link case and one stale scheduled fallback case.
3. Run the focused Vitest file, then stage and commit only the scoped remediation files.
