# Requirements

Acceptance criteria:
- Selecting a help role chip filters visible help results to articles whose `roles` include that role.
- Selecting `All` restores every matching help article.
- Keyboard active index and Enter navigation use the same filtered result set shown in the dialog.
- Empty role-filtered help results show the existing no-match state without hiding other sections.
