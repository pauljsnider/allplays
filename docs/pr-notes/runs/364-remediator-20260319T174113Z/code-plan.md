Thinking level: low. Single-thread remediation with an established repo pattern.

Implementation plan:
1. Update the `team-access.js` import in `edit-team.html` to include a version query.
2. Review the diff to confirm no unrelated changes.
3. Run the minimal validation available for this static site change.
4. Stage and commit only the scoped remediation files.
