# Code role plan

Thinking level: low (single-thread, localized test fix with clear reviewer guidance).

1. Edit first test in `tests/unit/live-game-replay-speed.test.js` to use elapsed value consistent with `elapsedAtSwitch` (10,000 ms).
2. Run targeted unit test command for this file.
3. Stage changed files (test + role notes) and commit with concise imperative message.
