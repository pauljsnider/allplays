# QA

Plan:
- Extend RegistrationDetail unit coverage for checkout creation failure and openPublicUrl failure.
- Add regression coverage for online checkout forms without registration options.
- Add assertion that checkout amount/currency comes from server-returned registration fee snapshot when present.
- Run focused Vitest file after edits.
