# QA Plan

- Inspect controller update path to verify summary is no longer gated by single-child matching.
- Run available JavaScript tests if present for parent dashboard RSVP behavior.
- Manual scenario: parent has siblings A and B on same event, A appears first, submit RSVP for B from calendar day modal, reopen modal and verify summary totals reflect B's response.
