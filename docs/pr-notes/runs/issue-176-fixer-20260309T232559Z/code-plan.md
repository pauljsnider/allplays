1. Add failing tests for a new `js/parent-membership-utils.js` module and for page/rules wiring.
2. Implement helper utilities for request IDs, transition checks, and merged parent-link state.
3. Implement request create/list/approve/deny helpers in `js/db.js`.
4. Update `parent-dashboard.html` to render a self-serve request form and request-status list.
5. Update `edit-roster.html` to render pending parent access requests and wire approve/deny actions.
6. Update `firestore.rules` for `membershipRequests`.
7. Run focused tests, then commit the targeted change set.
