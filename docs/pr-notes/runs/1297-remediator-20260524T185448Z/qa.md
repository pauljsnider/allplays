# QA

- Add/restore regression coverage that asserts deleteField() sentinel values are assigned when disabling recurrence on an existing practice.
- Verify recurring edits still preserve series metadata and do not call deleteField().
- Run focused Vitest unit tests for edit-schedule-practice-payload.
