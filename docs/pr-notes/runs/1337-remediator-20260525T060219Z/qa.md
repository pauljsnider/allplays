# QA notes

## Focused validation
- Unit-test explicit `sentCount: 0` preservation.
- Unit-test virtual recurring id metadata target resolution.
- Static regression test that the reminder panel/action uses `isTeamRsvpReminderManager` instead of broad `isTeamStaff`.
- Run the focused Vitest file and app TypeScript build.
