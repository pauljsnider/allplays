# Implementation Plan

1. Update `isSameLocalEvent` to parse `sourceEvent.startsAt` after `date`, `start`, and `startTime`.
2. Run lightweight module syntax validation.
3. Commit source and role-note files.
