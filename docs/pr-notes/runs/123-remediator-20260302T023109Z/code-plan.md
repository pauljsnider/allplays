# Code Role Notes

## Plan
1. Edit `js/team-access.js`:
   - Remove `coachOf` from `hasFullTeamAccess` full-access decision.
   - Update doc comment to match implemented full-access roles.
2. Keep change minimal and local to this helper.
3. Run a syntax check (`node --check js/team-access.js`).
4. Stage and commit all changed files.
