// Keep this value aligned with the legacy module and Firestore Rules. The
// cross-runtime transport test guards the shared contract without importing a
// browser Firebase adapter into the Capacitor-native transport.
export const COACHES_ONLY_GAME_NOTE_MAX_LENGTH = 5000;
